# NBA Fantasy Analytics App

Веб-приложение для анализа фэнтези баскетбольной лиги ESPN с расширенной аналитикой, анализом трейдов и симуляцией матчапов.

## 🚀 Возможности

- **Аналитика команд** - детальная статистика игроков команды с Z-scores
- **Анализ трейдов** - сравнение команд до и после обмена игроками
  - Анализ всей команды или только игроков трейда
  - Режимы: Z-scores и реальные значения
  - Места в симуляции до и после трейда
- **Симуляция матчапов** - симуляция "все против всех" с тремя режимами:
  - По данным матчапов (реальные результаты)
  - По статистике команд (avg)
  - По Z-score
- **Свободные агенты** - поиск и анализ свободных агентов
- **Все игроки** - полный список игроков лиги с Z-scores

## 📋 Требования

- Python 3.8+
- Node.js 16+
- npm или yarn
- Доступ к ESPN Fantasy Basketball лиге

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd NBA_MEGA_APP
```

### 2. Настройка Backend

Установите зависимости Python:

```bash
pip install -r requirements.txt
```

Или создайте виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

pip install -r requirements.txt
```

### 3. Настройка конфигурации

Создайте файл `.env` в корне проекта и укажите данные для подключения к ESPN API:

```env
ESPN_S2=your_espn_s2_token
SWID={your-swid-guid}
```

**Как получить ESPN_S2 и SWID:**
1. Войдите в ESPN Fantasy Basketball
2. Откройте Developer Tools (F12)
3. Перейдите в Application/Storage → Cookies
4. Найдите `espn_s2` и `SWID`
5. Скопируйте их значения в файл `.env`

**Примечание:** Файл `.env` уже добавлен в `.gitignore` и не будет попадать в репозиторий.

### 4. Настройка Frontend

Перейдите в директорию frontend и установите зависимости:

```bash
cd web/frontend
npm install
```

## 🚀 Запуск

### Windows

Просто запустите:

```bash
start_app.bat
```

Это запустит:
- Backend на `http://localhost:8000`
- Frontend на `http://localhost:5173`

### Ручной запуск

**Backend:**
```bash
uvicorn web.backend.main:app --reload --host 0.0.0.0 --port 8000
```

**Frontend:**
```bash
cd web/frontend
npm run dev
```

## 📖 Использование

### Аналитика команд

- Выберите команду и период статистики
- Просмотрите Z-scores всех игроков команды
- Используйте Punt Categories для исключения категорий из анализа

### Анализ трейдов

1. Выберите свою команду и команду оппонента
2. Выберите игроков, которых отдаете и получаете
3. Выберите период и Punt Categories
4. Просмотрите результаты:
   - Изменение Total Z-score
   - Детализация по категориям
   - Места в симуляции до и после трейда
   - Режимы: "Вся команда" или "Только трейд"

### Симуляция матчапов

1. Выберите режим симуляции:
   - **По матчапам** - использует реальные результаты матчапов
   - **По статистике (avg)** - использует средние значения игроков
   - **По Z-score** - использует нормализованные Z-scores
2. Выберите период (для режимов avg и Z-score)
3. Просмотрите рейтинг команд с винрейтом

### Свободные агенты

- Просмотрите список свободных агентов
- Фильтруйте по позиции
- Сортируйте по Z-score или категориям

## 🏗️ Структура проекта

```
NBA_MEGA_APP/
├── core/                    # Основные модули
│   ├── config.py           # Конфигурация
│   ├── league_metadata.py  # Работа с метаданными лиги
│   ├── z_score.py          # Расчет Z-scores
│   └── rank_players.py     # Утилита для ранжирования игроков
├── web/
│   ├── backend/
│   │   └── main.py         # FastAPI приложение
│   └── frontend/
│       ├── src/
│       │   ├── components/ # React компоненты
│       │   │   ├── Analytics.jsx
│       │   │   ├── TradeAnalyzer.jsx
│       │   │   ├── Simulation.jsx
│       │   │   └── ...
│       │   └── App.jsx
│       └── package.json
├── .env                     # Переменные окружения (ESPN_S2, SWID)
├── requirements.txt         # Python зависимости
├── start_app.bat           # Скрипт запуска (Windows)
└── README.md
```

## 🔧 API Endpoints

### Backend API (FastAPI)

- `GET /api/teams` - Список команд лиги
- `GET /api/analytics/{team_id}` - Аналитика команды
- `GET /api/all-players` - Все игроки лиги
- `GET /api/free-agents` - Свободные агенты
- `GET /api/simulation/{week}` - Симуляция матчапов
- `POST /api/trade-analysis` - Анализ трейда
- `GET /api/weeks` - Список недель сезона

Документация API доступна по адресу: `http://localhost:8000/docs`

## 📊 Категории статистики

Приложение использует 11 категорий:
- **Счетные**: PTS, REB, AST, STL, BLK, 3PM, DD
- **Процентные**: FG%, FT%, 3PT%, A/TO

## 🎯 Z-Score

Z-score показывает, на сколько стандартных отклонений игрок отличается от среднего по лиге:
- Положительный Z-score = выше среднего
- Отрицательный Z-score = ниже среднего
- Z-score = 0 = средний игрок

## 📝 Примечания

- Приложение требует активного подключения к интернету для работы с ESPN API
- Данные обновляются в реальном времени при каждом запросе
- Для работы необходимо иметь доступ к лиге ESPN Fantasy Basketball

## 🤝 Вклад

Приветствуются любые улучшения и предложения!

## 📄 Лицензия

Этот проект создан для личного использования.

## 🔗 Связанные проекты

- [espn-api](https://github.com/cwendt94/espn-api) - Python библиотека для работы с ESPN API

